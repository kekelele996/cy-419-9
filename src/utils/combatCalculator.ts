import type { Player } from '../models/player'; import type { Bug } from '../models/bug'; import { logGame } from './gameLogger'; export function damage(attacker:number, defense:number){ const value=Math.max(1, attacker-defense); logGame('COMBAT_DAMAGE',{value}); return value; } export function attackBug(player:Player, bug:Bug){ const value=damage(player.attack, bug.defense); bug.hp-=value; if(bug.hp<=0){ bug.status='dead'; player.exp+=12; logGame('BUG_DEAD',{type:bug.type}); } return value; }
