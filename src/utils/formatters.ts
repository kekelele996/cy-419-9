import { BugType } from '../constants/bug'; import { ItemType } from '../constants/item'; export const bugTypeText:Record<BugType,string>={null_pointer:'空指针',stack_overflow:'栈溢出',type_error:'类型错误',memory_leak:'内存泄漏',syntax_error:'语法错误'}; export const itemTypeText:Record<ItemType,string>={potion:'药水',syntax_fragment:'语法碎片',debugger:'调试器',refactor_scroll:'重构卷轴'}; export const rarityText:Record<string,string>={common:'普通',rare:'稀有',epic:'史诗'}; export const floorText=(level:number)=>['Sandbox','Staging','Production'][level-1]||'Unknown'; export const numberText=(value:number)=>Math.round(value).toString();
