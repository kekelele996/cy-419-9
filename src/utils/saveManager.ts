import { logGame } from './gameLogger'; const KEY='debugquest:save'; export function saveGame(data:unknown){ localStorage.setItem(KEY, JSON.stringify(data)); logGame('SAVE_WRITE',{key:KEY}); } export function loadGame<T>(fallback:T):T{ logGame('SAVE_READ',{key:KEY}); try { return JSON.parse(localStorage.getItem(KEY)||'') as T; } catch { logGame('SAVE_RECOVER'); return fallback; } } export function clearSave(){ localStorage.removeItem(KEY); }
