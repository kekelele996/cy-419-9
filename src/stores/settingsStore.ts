import { create } from 'zustand'; import { logGame } from '../utils/gameLogger'; export const useSettingsStore=create<{volume:number;theme:'terminal'|'paper';setVolume:(v:number)=>void;setTheme:(t:'terminal'|'paper')=>void}>((set)=>({volume:0.5,theme:'terminal',setVolume:(volume)=>{logGame('SETTING_VOLUME',{value:volume}); set({volume});},setTheme:(theme)=>{logGame('SETTING_THEME',{value:theme}); set({theme});}}));
