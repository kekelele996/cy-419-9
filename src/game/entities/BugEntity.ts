import Phaser from 'phaser'; import { BugType } from '../../constants/bug'; import { BUG_ANIMATIONS } from '../../constants/animations'; export class BugEntity extends Phaser.GameObjects.Rectangle { bugType:BugType; constructor(scene:Phaser.Scene,x:number,y:number,type:BugType){ super(scene,x,y,20,20,0xe4572e); this.bugType=type; this.setData('animation',BUG_ANIMATIONS[type]); scene.add.existing(this); } }
