import { useNavigate } from 'react-router-dom'; import { GameButton } from '../components/common/GameButton'; import { StatusBar } from '../components/common/StatusBar'; import { messages } from '../constants/messages'; export function GameOver(){ const nav=useNavigate(); return <main className="page"><StatusBar /><section className="panel"><h1>{messages.gameOver}</h1><GameButton onClick={()=>nav('/start')}>重新开始</GameButton></section></main>; }
