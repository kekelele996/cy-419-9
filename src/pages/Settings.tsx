import { useSettingsStore } from '../stores/settingsStore'; import { clearSave } from '../utils/saveManager'; import { GameButton } from '../components/common/GameButton'; export function Settings(){ const store=useSettingsStore(); return <main className="page panel"><h1>设置</h1><label>音量 <input type="range" min="0" max="1" step="0.1" value={store.volume} onChange={e=>store.setVolume(Number(e.target.value))}/></label><div className="mt-4"><GameButton onClick={()=>store.setTheme(store.theme==='terminal'?'paper':'terminal')}>切换主题</GameButton><GameButton onClick={clearSave}>重置存档</GameButton></div></main>; }
