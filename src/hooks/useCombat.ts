import { useDungeonStore } from '../stores/dungeonStore'; import { usePlayerStore } from '../stores/playerStore'; import { attackBug } from '../utils/combatCalculator'; export function useCombat(){ const player=usePlayerStore((s)=>s.player); const bugs=useDungeonStore((s)=>s.bugs); return { attackNearest:()=>{ const bug=bugs.find((item)=>item.status==='alive'); return bug ? attackBug(player, bug) : 0; } }; }
