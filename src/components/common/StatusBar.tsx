import { usePlayerStore } from '../../stores/playerStore'; import { useGameStore } from '../../stores/gameStore'; import { numberText } from '../../utils/formatters'; export function StatusBar(){ const p=usePlayerStore(s=>s.player); const score=useGameStore(s=>s.score); return <div className="panel flex flex-wrap gap-4"><span>{p.name}</span><span>HP {p.hp}/{p.max_hp}</span><span>MP {p.mp}/{p.max_mp}</span><span>Lv {p.level}</span><span>Score {numberText(score)}</span></div>; }
